# BrainX 数据增长、引用与保留责任只读盘点

> 上级入口：[仓库重构施工总手册](2026-09-22-refactor-agentic-ranking-manual.md)阶段 02 ·
> [文档书](README.md)
> 规格：[024 数据增长与保留责任只读盘点](../specs/024-retention-inventory/spec.md) ·
> [025 清理路径隔离与显式策略 dry-run](../specs/025-retention-dry-run-safety/spec.md)
> 日期：2026-09-23；状态：本地只读盘点与安全 dry-run 已实现，生产盘点、TTL 审批、责任人分配与恢复演练未完成。

## 1. 范围与证据边界

新增 `npm run retention:audit`，使用 SQLite 只读模式输出 `retention-inventory-v1`。它不调用会迁移
或播种的数据库入口，不提供 `--apply`，不输出正文、原始 JSON、业务 ID、顾问或群标识。测试
证明盘点前后数据库文件摘要不变，缺表时也不会创建 schema。

2026-09-22 22:46 +08:00 对仓库当前本地工作副本执行一次聚合盘点：数据库文件 329,367,552
字节，执行前后文件大小和修改时间未变。以下数字只证明该本地副本的结构与规模，不能代替生产
实测，也不能作为删除授权。

## 2. 本地聚合结果

| 类别 | 行数 / 聚合体积 | 引用与能力 | 结论 |
|---|---:|---|---|
| `ttc_field_reports` | 42,947 行；`report_json` 约 122.8 MiB | 42,947 行均有父 `sync_runs`；按顾问、schema、完整报告精确重复为 0 | 当前最大已测大字段；不能用“完全相同报告”直接得到清理集合 |
| `sync_runs` | 49,268 行；错误摘要约 0.2 MiB | 2,734 个运行被当前职位引用；42,947 个有字段报告；18,334 条决策运行指向同步快照 | 三类引用会重叠，不能用总数相减推导无引用行 |
| 限流跳过运行 | 813 行 | 关联推荐项为 0 | 是运营审计，不是决策快照；TTL 和异常样本保留仍未批准 |
| 推荐/特征快照 | 6,600 行；解释与快照字段约 13.6 MiB | 660 项有关联曝光，其中 120 项已真实下发；当前副本没有业务结果或事件引用 | 本地副本缺 `feature_snapshot_json` 列和追加负反馈事件表，不能据此验证最新评估证据 |
| 原始上下文 | 25,066 行；约 36.9 MiB | `job_facts` 23,565 行、飞书消息 951 行、事实草稿 550 行 | 含受限来源证据；必须核对活动引用、隐私删除传播与恢复流程 |

日增长明细由 CLI 按表输出最近最多 30 个有数据日期。报告只包含日期与行数，不包含来源内容或
主体标识。字段报告在本地体积中占比显著，但报告内容变化可能来自真实字段覆盖差异；在引入规
范化内容哈希和差异样本之前，不能把它们判为重复垃圾。

## 3. 保留责任矩阵

| 类别 | 权威性 | 建议责任角色 | 当前状态 | 删除前条件 |
|---|---|---|---|---|
| 字段报告 | 派生审计证据 | 数据负责人、工程负责人 | 人员未分配；TTL 未批准 | 保存差异样本、审计引用和内容哈希口径 |
| 同步运行 | 权威血缘 | 数据、工程、运维负责人 | 人员未分配；TTL 未批准 | 水位、当前职位、决策快照和故障调查均不受损 |
| 限流跳过 | 运营审计 | 工程、运维负责人 | 人员未分配；TTL 未批准 | 保留窗口聚合、错误样本与异常检测依据 |
| 推荐/特征快照 | 决策证据 | 业务、数据、工程负责人 | 人员未分配；TTL 未批准 | 活跃回放、真实曝光、冻结评估集、反馈和事故调查全部核对 |
| 原始上下文 | 受限来源证据 | 数据、安全、业务负责人 | 人员未分配；TTL 未批准 | 授权、活动引用、删除账本、缓存/索引和备份恢复传播均通过 |

所有类别的恢复点状态仍为 `UNVERIFIED`，所以 CLI 固定输出 `deletion_ready=false`，不生成候选
主键、删除 SQL 或截止日期。

## 4. 清理路径隔离结果

1. 推荐事务已移除 `pruneRecommendationHistory()` 及其环境变量。单轮仍最多持久化既定数量，
   但生成推荐不再顺带删除旧轮次；历史生命周期只允许由独立流程管理。
2. `bin/brainx-retention.mjs --apply` 现在先返回 `RETENTION_APPLY_DISABLED`，再决定是否触库；
   不带 `--apply` 的旧调用转到 `retention-inventory-v1` 只读盘点，不建临时表、不迁移、不播种。
3. 测试以不存在的数据库路径证明 apply 在打开文件前失败，并以文件摘要证明新旧只读入口均不
   修改数据库。旧 Top 20 曝光推断、DELETE、checkpoint 和 VACUUM 路径均已移除。
4. 本地副本仍未具备最新特征快照列与负反馈事件表。没有恢复点时不得借盘点之名执行迁移；应
   先由阶段 00 补齐脱敏副本恢复验证，再重新运行相同只读报告。

## 5. 显式策略 dry-run

`npm run retention:plan -- --policy <策略文件> [--db <只读副本>]` 输出固定
`retention-plan-v1`。策略必须提供规范 ISO `as_of`、推荐 TTL、每顾问最近保留轮次和节流审计
TTL，例如：

```json
{
  "contract_version": "retention-policy-v1",
  "as_of": "2026-09-23T00:00:00.000Z",
  "categories": {
    "recommendation_snapshots": { "ttl_days": 30, "keep_latest_runs_per_consultant": 3 },
    "throttled_runs": { "ttl_days": 7 }
  }
}
```

报告只含聚合总数、年龄命中数、保护原因和候选数。推荐候选须同时满足：早于 TTL、超出最近
保留轮次、无展示记录、无真实下发、无业务结果、无决策事件、无追加负反馈事件。缺冻结特征
列或任一引用表时返回 `BLOCKED_SCHEMA_CAPABILITY`，不猜测候选。字段报告、同步运行和原始
上下文固定为 `BLOCKED_POLICY_NOT_IMPLEMENTED`。

候选计数不是执行授权。报告始终返回 `execution_supported=false` 和
`execution_ready=false`，不含主键、内容、SQL 或执行命令；本单元没有归档和删除实现。

## 6. 下一单元准入条件

后续 dry-run/执行器至少要先满足：责任人具名、TTL 与例外获批；一致性备份可恢复；引用集合覆盖
曝光、结果、事件、反馈、实验和事故冻结；按稳定主键分批；默认 dry-run；执行需单独确认令牌与
停止开关；每批有删除审计和可恢复标记；磁盘不足、重复执行、中断恢复和活跃回放均有失败测试。

在这些条件完成前，允许继续测量和设计，不允许清理真实数据。
