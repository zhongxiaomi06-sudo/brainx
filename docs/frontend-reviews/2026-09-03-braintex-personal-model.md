# 顾问个人模型正式接入记录

> 上级入口：[前端审核台账](README.md)

## 审核身份

- 审核日期：2026-09-03
- Storybook 场景：`业务组件/个人模型/Settings Entry`
- 对应 commit：本记录所在 commit
- 审核范围：供应商、模型、API Key、数据处理同意、保存、替换和停用界面

## 用户结论

- 已确认：产品方向必须允许不同顾问配置自己的模型，StepFun 不能作为全员默认。
- 未确认：本轮具体视觉、文案、桌面与手机布局尚未由用户复看。
- 正式接入授权：用户已明确要求继续施工、正式部署和开放六人；视觉状态仍独立记为未审核。

## 数据与动作边界

- 真实字段：个人 Agent 就绪、供应商、模型、状态、同意版本和配置时间。
- 缺失字段：API Key 不允许由业务 API 读取；供应商余额和模型可用性不在状态接口中。
- 后端依赖：`GET/PUT/DELETE /api/v1/model-profile`、飞书签名 session、OpenClaw direct peer binding。
- 允许动作：本人保存、替换或停用个人模型；管理员只能读取非敏感就绪状态。

## 状态证据

- 正式入口：`SettingsCenterReview → PersonalModelPanel`；`/brainx → ?settings=model`。
- 发布环境与版本：未发布。
- 自动验证：前端静态契约 2/2；组件清单；桌面 685px 与手机 300px 内容区均无横向溢出。
- 真实数据验证：仅用脱敏 profile 验证页面；真实 API Key 未进入测试、仓库或 Storybook。

## 未完成项

- [ ] 用户复看视觉和文案。
- [ ] 生产环境部署后验证 OAuth 深链和错误状态。
- [ ] 至少两位凭据所有者分别配置不同供应商并完成飞书问答。
