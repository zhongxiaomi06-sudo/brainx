# 016 — 实现计划

> 上级规格：[spec.md](spec.md)

1. 复用 `launchRecruitingWorkflow` 作为每日卡、对话工具和工作台的项目群入口。
2. 将 `brainx_accept_job` 的职责改为“接单 + 建群”，移除隐式 OpenMai 启动。
3. 给对话推荐卡增加携带明确项目编号的“接单并建群”命令按钮。
4. 对建群成功、部分失败、不自动找人和按钮命令补回归测试。
5. 同步原生飞书审核台账、施工清单和 Storybook 边界说明。
