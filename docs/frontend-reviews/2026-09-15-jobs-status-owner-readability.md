# 全部职位状态与负责人可读性复核

> 上级入口：[前端审核台账](README.md)

## 审核身份

- 审核日期：2026-09-15
- Storybook 场景：`JobsWorkspaceReview / DesktopReview`
- 对应 commit：本记录随 `fix(frontend): 职位表改用可读状态与负责人` 一并提交
- 审核范围：正式“全部职位”列表的工作流状态文案与负责人列

## 用户结论

- 已确认：用户要求连续推进并在每项修改后用真实页面测试、截图；本轮截图已发当前对话。
- 未确认：用户尚未逐字确认“待判断”与负责人姓名的最终视觉样式。
- 正式接入授权：允许；改动直接作用于已接入的生产组件，不改变后端状态或动作。

## 数据与动作边界

- 真实字段：`workflowState`、`ownerName`。
- 缺失字段：负责人为空时仍显示“待确认”，不猜测归属。
- 后端依赖：无新增依赖；`PENDING` 数据语义不变，仅把展示文案从容易造成紧迫感的“待处理”改为“待判断”。
- 允许动作：既有筛选、整行打开详情和加入项目动作保持不变。

## 状态证据

- 正式入口：主导航“全部职位”中的嵌入式 `JobsWorkspaceReview`。
- 发布环境与版本：本地开发环境；目标环境未发布。
- 自动验证：Storybook 86/86；生产构建成功。
- 真实数据验证：本地真实接口返回 23245 个待判断职位，首屏渲染 100 行；页签与行内旧“待处理”均为 0，负责人显示完整姓名（如 Eva 郑晓玲、George 刘明），截图已发当前对话。

## 未完成项

- [ ] 用户复看状态文案、负责人列宽和姓名截断效果。
- [ ] 目标环境发布后复核相同真实字段与空值场景。
