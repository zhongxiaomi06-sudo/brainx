# OpenMai 候选人表格卡复审

> 上级入口：[前端审核台账](README.md)

## 审核身份
- 审核日期：2026-09-08
- Storybook 场景：飞书原生卡片不在 React Storybook 中复制，以真实飞书客户端验收
- 对应 commit：本记录所在提交
- 审核范围：OpenMai 首轮候选人总览卡的排版、消息数量与末尾动作

## 用户结论
- 已确认：原始 Markdown 表格在飞书中显示竖线和分隔符，排版被退回；用户要求改为整洁表格，并在末尾只保留一个按钮。
- 未确认：新版双列表格的最终视觉、窄屏表现和按钮点击后的完整操作。
- 正式接入授权：允许按上述范围修改本地正式投递链路，待用户在真实群复看。

## 数据与动作边界
- 真实字段：候选人姓名、OpenMai AI 初评、候选引用、真实简历 URL（若上游提供）。
- 缺失字段：本轮真实结果没有简历 URL；不得制造 PDF 或下载入口。
- 后端依赖：OpenMai 结构化 `BRAINX_CANDIDATES_V1` 结果与飞书互动卡片发送接口。
- 允许动作：总览卡末尾打开已鉴权工作台；只有取得真实 PDF 的候选人才创建独立附件话题。

## 状态证据
- 正式入口：`src/openmai-delivery.js` 的真实项目群投递 worker。
- 发布环境与版本：本机代码，未发布服务器。
- 自动验证：候选人投递专项覆盖表头、候选行、原始 Markdown 不外露、单按钮和 PDF 条件话题。
- 真实数据验证：使用项目 `JCU85O0` 的 9 名真实结构化候选人向真实项目群发送新版预览；飞书返回成功消息 ID，卡片含 10 行表格元素与 1 个动作。

## 未完成项
- [ ] 用户在真实飞书客户端确认新版视觉。
- [ ] 在取得真实 PDF 的搜索结果上验证附件话题。
- [ ] 在手机或窄屏飞书客户端验证双列布局。

## 2026-09-08 第二轮状态更新：逐人操作与 TTC 回查

- 用户追加要求：每一名候选人后都要有独立“查看”和“发送简历”按钮，不能只在整张卡末尾提供总按钮。
- 正式接入：总览改为“候选行 + 独立操作区”重复布局；飞书不允许把 action 放进 column，真实预览曾返回 `200410`，因此按钮位于对应候选行正下方。
- 简历来源：OpenMai 没有返回 URL 时，点击后按候选编号实时查询 TTC 人才库附件；没有附件时明确提示，不伪造链接。TTC 凭据仅发往 API 首跳，文件域跳转不携带 Bearer；文件限制 12 MiB 且必须通过 PDF 内容校验。
- 群安全：全局仍要求群消息 @ 机器人；仅系统已登记且完成 sender allowlist 的项目群关闭 mention 门，以兼容飞书卡片回调本身不带 @ 元数据。
- 真实验证：项目 `JCU85O0` 的新版卡片在真实飞书群成功显示 9 行、9 组双按钮；点击谢伟峰的“发送简历”后，机器人从 TTC 回查并发送 `169.2 KB` 的真实 PDF，随后回复“简历已发送”，工具审计状态为 `SUCCEEDED`。
- 当前状态：桌面端布局与单人真实 PDF 动作已验证；手机/窄屏、无附件提示和服务器发布仍未验证。
