# 每日推荐卡一键接单并建群复核

> 上级入口：[前端审核台账](README.md) · [文档书总目录](../README.md)

## 审核身份

- 审核日期：2026-09-10
- Storybook 场景：不适用；这是飞书原生 legacy v1 卡片，不复制 React 仿真组件
- 对应 commit：以本次原子提交为准
- 审核范围：每日自动推荐卡每个职位的主动作，以及点击后的加入项目、接单和建群链路

## 用户结论

- 已确认：用户明确要求不再先进入工作台；推送岗位后应在卡片上直接点击接单，并随即创建项目群。
- 未确认：真实飞书中的按钮密度、手机布局、成功页、建群成员和权限失败提示尚未目测。
- 2026-09-10 第二轮真机：桌面卡片已显示 5 个职位及每行“接单并建群/查看职位/回放/忽略”；首次点击因 `BRAINX_BASE_URL` 尾斜杠生成双斜杠路径而返回 404，用户截图确认后已修复 URL 归一化，待重发新卡复测建群。
- 正式接入授权：允许接入每日推荐卡；工作台保留为查看职位和异常兜底。

## 数据与动作边界

- 真实字段：顾问、职位 `project_id`、公司、岗位、冻结推荐、项目归属、飞书身份绑定与既有项目群状态。
- 缺失字段：不新增或推断任何职位字段。
- 后端依赖：`BRAINX_FEEDBACK_SECRET`、公网 HTTPS `BRAINX_BASE_URL`、飞书应用建群权限、ACTIVE 身份绑定和 OpenClaw 群准入配置。
- 允许动作：当天/次日有效的 HMAC 链接将本次点击视为明确确认，依次加入本人项目、执行接单、幂等创建职位唯一项目群、投放职位卡；不自动启动 OpenMai 或 SuperMai。
- 安全降级：缺签名密钥时不显示一键写按钮，只显示打开职位；链接不得转发，篡改、过期或缺项均失败关闭。

## 状态证据

- 正式入口：`src/push.js` 每日推荐卡 → `GET /api/v1/feedback/quick?action=launch` → `launchRecruitingWorkflow`。
- 发布环境与版本：未发布。
- 自动验证：卡片按钮/签名、加入项目、接单、建群、投放、重复点击幂等和“不启动找人”专项测试通过。
- 真实数据验证：验证失败后修复中；Dykes 真实账号收到 5 职位卡并点击，首轮暴露双斜杠 404，尚未完成修复后的建群成员与重复点击验证。

## 未完成项

- [ ] 在目标环境设置独立 `BRAINX_FEEDBACK_SECRET` 并重启 BrainX。
- [ ] 用真实顾问收到一张新生成的每日推荐卡，验证桌面与手机按钮布局。
- [x] 桌面端收到 5 职位真卡并验证每行四个动作；修复尾斜杠导致的 `//api/v1/feedback/quick` 404。
- [ ] 点击一次核验本人、机器人和已绑定项目协作者成员，点击第二次确认不重复建群。
- [ ] 在无建群权限或身份绑定缺失的测试身份上验证明确失败提示。
- [ ] 回群后选择 OpenMai/SuperMai，确认卡片点击本身没有创建找人任务。

## 相关文档

- [OpenClaw 招聘闭环](../2026-09-03-openclaw-recruiting-loop.md)
- [部署编排](../DEPLOYMENT.md)
- [内部 Storybook 组件库](../storybook-component-library.md)
- [上传前完整验证](../standards/PRE_PUSH_VERIFICATION.md)
