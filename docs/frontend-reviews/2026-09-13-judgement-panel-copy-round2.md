# 判断面板文案精简·第二轮（合并行与事实区白名单）

> 上级入口：[前端审核台账](README.md)

## 审核身份

- 审核日期：2026-09-13
- 用户结论：用户在前端看到仍为旧文案后，确认上一轮修复「没有部署上」，并选定 **B 版**（事实区只留决策必需行，`备注` 完整保留，层级与可信度合并为一行「判断依据」）。
- Storybook 场景：业务组件/辅助流程（Facts）
- 审核范围：判断抽屉「当前事实」区的事实行集合与合成行；不改接口契约与数据字段语义
- 对应 commit：见 `docs/AGENT_COMMIT_LOG.md` 2026-09-13 条目

## 背景与上一轮的实际缺口

- 第一轮（`e62c2ce`，2026-09-12）已删除规则版本号、快照 UUID、原始枚举、环境变量提示与来源标签，
  并把 `UNKNOWN` 显示为「待确认」；但**生产前端从未重新构建**，线上 `workbench-Csz5cgVO.js`
  仍命中 `BRAINX_TALENT_SUPPLY`、`事实可信度规则`，用户看到的仍是旧面板。
- 第二轮按用户「只留下几行精简的文字，备注信息留下来」的要求，把事实区收敛为白名单，并新增合成行。

## 改动摘要

1. **事实区白名单**（新模块 `app/workbench-facts-model.ts`，纯函数便于 node 测试）：
   只展示 `职位关系 / 主做顾问 / 职位状态 / 当前阶段 / 剩余 HC / 历史 Pipeline / 下一步动作 / 城市 / 备注`（有值才出现）。
2. **合成行「判断依据」**：由 `决策层级` + `事实可信度` 合成一行（如「本周关注 · 数据部分缺失」），
   替代原「决策层级 / 决策层级原因 / 事实可信度 / 事实更新时间」四行。
3. **不再进入判断面板**（仍保留在数据层供队列卡片与排序使用）：`数据来源`、`决策层级`、`决策层级原因`、
   `事实可信度`、`事实可信度规则`、`事实更新时间`、`最近活动`、`最近活动时间`、`最近活动来源`、`Offer 状态`、`入职状态`。
4. `备注`（JD + 小麦同步画像）完整保留，不做截断与改写。

## 数据与动作边界

- 数据层不变：`job.facts` 仍携带全部键，队列卡片（`workbench-today.tsx`）继续用
  `最近活动 / 最近活动时间 / 事实更新时间 / 决策层级 / 事实可信度`，排序与层级派生不受影响。
- 后端依赖：无新增；允许动作：无新增。
- 事实编辑：`factFieldByLabel` 与「恢复同步值」逻辑不变，白名单只影响只读展示。

## 状态证据

- `tsc --noEmit` 0 错误；`npm test`（btex-frontend）**46/46**（新增 `judgementRows` 白名单与合并行回归测试）。
- 无头渲染核对（worktree 内 `storybook:build` 产物 + Playwright/系统 Chrome 打开 Facts story）：
  行标签 = `["职位关系","当前阶段","剩余 HC","历史 Pipeline"]`；`数据来源 / 入职状态 / Offer 状态 /
  最近活动 / 事实更新时间 / 决策层级 / 事实可信度 / 事实可信度规则 / 最近活动来源` **全部不再出现**。
- 交互测试（`storybook:test`）在临时 worktree 内无法运行（node_modules 为符号链接、位于服务根目录外，
  属环境限制），改由 CI（干净检出）执行；本地以纯函数单测 + 无头渲染断言替代。
- 发布与线上核对证据见台账对应行与 `AGENT_COMMIT_LOG`。

## 未完成项

- [ ] 用户用真实数据复看判断面板（本记录不得替代用户审核维度）
- [ ] 目标环境真实数据逐行验证（备注保留、判断依据合并行、候选供给兜底文案）
