# 多 Agent 协作准则

本文件适用于仓库根目录及其全部子目录。任何 Agent 或自动化协作者在读取、修改、提交或上传本仓库内容前，都必须先阅读并遵守本准则。

读完本文件后，必须继续阅读 [文档书总目录](docs/README.md)，并根据其中的“任务阅读路由”选择本次任务的必读文档。所有任务都必须阅读 [上传前完整验证](docs/standards/PRE_PUSH_VERIFICATION.md)，以便在实现开始前就知道最终验收标准。

## 1. 核心原则

1. 每次开始工作前先从第一性原理确认：真实目标是什么、哪些是已验证事实、哪些只是沿用假设、不可削减的约束是什么、最小可行改动是什么。
2. 同一时间只允许一个 Agent 在共享工作区内修改、提交或上传代码。
3. 检测到其他 Agent 正在工作时，必须立即停止写操作，等待对方完全结束并释放工作锁；不得抢占、覆盖、暂存、提交或上传对方的改动。
4. 不得擅自清理、回滚、暂存或改写来源不明的工作区改动。发现非本人产生的改动时，一律视为其他协作者仍可能在工作。
5. 每次代码改动完成并通过必要检查后，必须立即创建独立 commit；不得把多个无关任务积攒在一个 commit 中。
6. 每个 commit 都必须使用简明中文说明，并同步更新 `docs/AGENT_COMMIT_LOG.md`，方便后续审查。
7. 只有确认工作区无其他 Agent 活动、改动已提交且验证通过后，才允许上传（push）。

## 2. 第一性原理与最小实现

理解任务和相关代码的真实调用链后，按顺序判断，并在第一个可行层级停止：

1. 这项改动是否真的需要存在，还是可以删除错误假设或复用现有行为？
2. 仓库中是否已有可复用的函数、类型、组件或模式？
3. 标准库、语言或平台原生能力是否已经解决？
4. 已安装依赖是否足够？避免为少量代码增加新依赖。
5. 只在以上方案都不成立时，编写能正确完成任务的最小代码。

优先根因修复而非症状补丁；优先删除而非新增、清晰而非炫技、最短正确 diff 而非未来式脚手架。不得以“精简”为理由削减信任边界校验、防止数据丢失的错误处理、安全、可访问性、硬件校准、用户明确要求或必要验证。非平凡逻辑至少留下一个能在回归时失败的最小可运行检查。

本节改编自 MIT 许可的 [Ponytail](https://github.com/DietrichGebert/ponytail/blob/main/skills/ponytail/SKILL.md)。核心含义是高效而非草率；最小化的是维护负担，不是问题理解和正确性。

## 3. 开始工作前

依次执行以下检查：

1. 阅读本文件以及目标目录内更具体的 `AGENTS.md`（若存在）。
2. 运行 `git status --short --branch`，确认当前分支、未提交改动和上游状态。
3. 检查是否存在 Git 操作状态或锁文件，例如 `.git/index.lock`、`MERGE_HEAD`、`REBASE_HEAD`、`CHERRY_PICK_HEAD`。
4. 先判断是否确实共享同一物理工作区：独立 clone/worktree 不使用本地工作锁；共享工作区必须检查 `logs/agent-work.lock`：
   - 若目录存在，视为其他 Agent 正在工作，立即停止写操作并等待。
   - 若目录不存在，使用原子操作 `mkdir logs/agent-work.lock` 获取工作锁。
   - 获取锁后，在 `logs/agent-work.lock/owner` 中记录 Agent/任务标识、开始时间和计划修改范围。
5. 获取锁后再次运行 `git status --short`。如果出现非本人预期的改动，停止工作并确认来源，不得覆盖。

等待期间只允许进行不会改变仓库、索引、分支或远程状态的只读检查。不得通过删除锁目录来抢占工作。只有能够确认锁的创建者已异常退出时，才可以在获得用户许可后清理陈旧锁。

## 4. 修改过程

1. 修改范围必须与当前任务直接相关；不要顺手改动无关文件。
2. 保留用户和其他协作者已有的改动，不得使用破坏性命令覆盖共享工作区。
3. 长任务应拆分为可验证的小步，每一个完整、可独立审查的小步都应尽快提交。
4. 提交前检查 `git diff` 和 `git diff --cached`，确保没有夹带密钥、账号、生成物或他人改动。
5. 根据改动风险运行相应的测试、类型检查、格式检查或构建；无法运行时必须在提交记录中注明原因。
6. 项目代码以外的流程、规范、架构决策、协作机制、运维方法或重要配置变更，必须新建或更新 Markdown 文档，并登记到 `docs/README.md`；不得只留在聊天记录中。
7. 文档必须链接回上级目录，并链接相关规则或操作文档。一个主题只保留一个权威说明，其他文档通过链接引用，避免复制后产生冲突。
8. 每个手写源代码、测试或配置文件不得超过 500 个物理行。新文件必须直接合规；现有超限文件不得继续增长，并须在下一次功能性修改前先拆分。自动生成文件、依赖锁文件、固定测试数据和第三方代码可例外，但必须能由路径或生成声明明确识别，Agent 不得自行把普通源码标记为例外。
9. 不得通过整文件关闭 Lint、压缩为超长单行或把代码搬进字符串来规避 500 行限制；拆分必须形成职责清楚、可独立测试的模块。

## 5. Commit 与中文记录

每次改动完成后立即执行以下流程：

1. 在 `docs/AGENT_COMMIT_LOG.md` 顶部追加一条中文记录，内容至少包括日期、提交标题、改动摘要和验证结果。
2. 只暂存本次任务涉及的文件以及本次日志记录。
3. 创建一个原子 commit。推荐格式：`类型(范围): 中文摘要`，例如 `fix(auth): 修复登录状态失效问题`。
4. 提交后立即运行 `git status --short --branch` 和 `git show --stat --oneline HEAD`，确认提交正确且工作区符合预期。
5. 如果验证发现问题，继续修复并创建新的 commit；不得通过静默改写已共享的提交掩盖历史。

常用类型包括 `feat`、`fix`、`refactor`、`test`、`docs`、`build`、`chore`。

## 6. 上传（Push）前的强制检查

上传前必须重新确认：

1. 自己仍持有 `logs/agent-work.lock`，且锁中的任务标识属于当前工作。
2. 没有其他 Agent、Git 操作或来源不明的新改动正在进行。
3. `git status --short` 符合预期，所有计划上传的代码都已提交。
4. 本地分支与远程分支关系清楚；必要时先安全获取远程状态并处理冲突。
5. 测试和提交记录已完成。

任一条件不满足时，不得 push。检测到其他 Agent 正在工作时，必须等待其完全结束、提交并释放锁后，重新检查仓库状态，再决定是否上传。

## 7. 结束与交接

1. 向用户用中文简要说明本次 commit 的标题、主要改动和验证结果。
2. 确认没有未完成的 Git 操作，工作区没有遗漏的本任务改动。
3. 完成 commit 和获准的 push 后，删除自己创建的 `logs/agent-work.lock` 以释放工作区。
4. 如果任务中断或无法完成，保留现场并明确告知用户；不要提交半成品，也不要删除他人的锁或改动。

## 8. 冲突处理优先级

用户当前明确指令 > 更深层目录中的 `AGENTS.md` > 本文件。若指令之间存在冲突或会导致数据丢失，停止写操作并向用户确认。
