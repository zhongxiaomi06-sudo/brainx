# 冷启动第一批 · 数据评判 v1（2026-09-24）

> 上级目录：[文档书总目录](README.md) ｜ 关联：[冷启动基线](2026-09-23-cold-start-baseline.md)、[规则 v2 与画像](2026-09-24-cold-start-rules-v2-profiles.md)、[顾问画像匹配 v1](2026-09-24-consultant-profile-matching-v1.md)、[specs/022 推送精准度](../specs/022-client-feedback-signals/spec.md)
>
> 本文是用户要求「按照这个画像给出第一版本的数据评判，数据都要给出，然后节点都明确」的交付物。口径：全部数字取自生产库（ECS `/opt/brainx/data/brainx.db`，2026-09-24 快照）+ 渠道归因 v3 分析；每节标注证据来源。

## 0. 一句话结论

**画像与匹配的「数据地基」已过验收（6/14 节点完成），但决策闭环的另一半（人工确认 1,737 条 pending → LTR 重导 → routed push）尚未启动；服务率 5.4% 是当前最大单点问题，已由 specs/022 立项但未施工。**

## 1. 全量数据快照（回填后，2026-09-24T05:47Z）

| 表 | 值 | 说明 |
|---|---|---|
| job_facts_drafts.pending | **1,737** | 1,644（GLM 回填）+ 93（本周网关新增） |
| job_facts_drafts.confirmed | 4 | 人工确认几乎未启动 |
| job_facts_drafts.rejected | 6,877 | 含 8,131 条 GLM 判定（NOT_JOB 6,041 / SUSPECTED 84 / 降级） |
| judgment_drafts | 14 | |
| job_outcomes | 11 | |
| recommendation_impressions | 9,980 总 / **540 served（5.4%）** | served 率较基线 4.5% 微升但仍异常低（specs/022 C 层诊断项） |
| recommendation_feedback | 72 | 全部 NOT_INTERESTED（labeling-standard-v1 口径） |
| client_profiles | **141 家 / 85 活跃** | 活跃口径 real_job_count ≥ 3 |
| consultant_profiles | 9 行（分析口径 7 人） | 2 行为无操作记录的空壳顾问 |
| lark_messages | **18,694 条 / 196 会话** | gateway 17,772（172 群）+ backfill 922（24 群，本次新增） |

## 2. 顾问画像 v3（渠道归因修正后的可信版）

v1 画像被「launch 自动化代操作」污染。v3 按 idempotency_key 前缀把 84 条 ACCEPTED 拆成真人操作（`bot:accept:` 卡片 / `web:`）与脚本代点（`accept_*`/`mia-accept`/`brainx-accept`/`cj_*`/`jptlm`，权重 ×0.5）、launch 自动化（仅归因不记分）。

| 顾问 | 本人卡片 | Web | 代点 | launch | 岗位族画像（v3） |
|---|---|---|---|---|---|
| mia | 5 | 4 | 4 | 11 | 算法 13.5（9 次本人操作） |
| shanon | — | 14 | — | — | 算法 16 / 未分类 6 / 产品 2 |
| felix | — | 12 | 5 | 8 | **运营 8 = 算法 8**（双栖）/ 市场 5 |

**关键修正（经用户本人复核确认）**：
1. v1「mia 算法深度型：集中度 45.8%」——**用户本人否认其中部分发言**，核实为 launch 代操作污染；修正后 mia 有效本人操作 9 次。
2. v1「felix 算法主导」——证伪，真实是运营/算法双栖。
3. v1「york 承接王 189」——证伪，188 条是 ttc-owner 同步不是真人承接。
4. **84 条 ACCEPTED 中仅 49 条（58%）是顾问真人操作**——所有基于 ACCEPTED 的画像必须用 v3 权重，v1 已废弃。

## 3. 客户画像与市场结构

- Top 客户（GLM 窗口 REAL_JOB 信号）：具身智能群 200 / 光轮智能 74 / 清雁科技 59 / AI 医疗 58 / 及传媒 48 / 超衍智能 40。
- 市场结构（13 族，第一批 1,914 条信号）：算法 271 / 管理岗 113 / 市场增长 101 / 运营 90 / 后端 86 / 产品 66 / 销售BD 47 / 设计 28 / 前端 28 / 硬件机器人 22 / 数据 17 / 其他技术 17 / 测试QA 13。
- **结构性缺口**：管理岗（113）+ 市场增长（101）需求合计 214 条，但顾问供给只有 felix 一个人沾运营/市场边；算法需求 271 对应 shanon+mia 两个算法向顾问——**算法侧供给相对充足，管理岗/市场增长侧近乎裸奔**。

## 4. 匹配覆盖评判（v1 评估的三个修正）

- v1 覆盖率 52/85（61%）→ v3 纠正后 **≈93%**：29 个「未覆盖」客户实为岗位词表未分类（未分类 6 条族归属缺失），不是顾问盘子问题。
- 覆盖的前提画像全部换 v3 权重；v1 的「承接量排名」不成立（见 §2）。
- routed push 的前置（画像 × 岗位族匹配）在数据面已就绪，缺的是 LTR v2 特征层（specs/022 US2-4）。

## 5. 第二批回填与增量验证（本次新增）

**背景**：bot 网关只覆盖 bot 在群的 172 个会话（事件驱动，`src/gateway/lark-messages.js`）。用户身份可见 86 群中 69 群不在库内，其中 24 个为客户/项目群。

**执行**（`bin/brainx-lark-backfill.mjs` + migration 0056 `origin` 列，origin='backfill'，INSERT OR IGNORE 幂等，dry-run 默认）：

| 项 | 值 |
|---|---|
| 拉取 | 24 群 page-all，934 条原始，has_more 全 False |
| 落库 | **922 条**（12 条已撤回/非法被过滤），0 重复，事务化，gateway 数据未动 |
| 信号密度 | 232/934 = **24%**（第一批 15%——客户项目群密度符合预期地更高） |

**第二批族分布（规则口径，验证用）**：硬件机器人 92 / 产品 66 / 管理岗 57 / 算法 52 / 市场增长 40 / 销售BD 26 / 后端 21（注：单群标题词会串进转发正文，硬件 92 含群名偏置，生产抽取前需标题抑制）。

**13 家新公司确认进入数据面**（第一批完全未覆盖）：思博威视、自然映射、智子芯元、Unipat、海马云、巨日禄、国投、物外智趣、普罗资本、穿越者、硅羽、优艾智合、懂车族。

**验证结论**：
1. 需求带增量：**智能影像/机器人方向的硬件产品经理**是第二批最集中的新需求（思博威视/优艾智合/硅羽/乐享/穿越者/物外智趣 6 家），第一批画像里硬件机器人仅 22 条——市场结构认知需按此修正。
2. 管理岗缺口二次确认：脑利（CTO/后端/财务总监）、巨日禄（技术负责人）、物外智趣（语音对话负责人）——管理岗 57 条信号无对应顾问。
3. 规则误报模式与第一批一致（如销售群转发命中硬件词），进一步证明「规则层只能做召回，语义判定必须 LLM 层兜底」（specs/022 决策①成立）。

### 5.1 补充验证（同日）：served 5.4% 的结构性根因实锤

对「真人 ACCEPT 的职位 ↔ 其推荐曝光记录」做 JOIN 验证（生产库）：

| 项 | 值 |
|---|---|
| 真人 ACCEPT（bot:accept: + web:）| 49 条（卡片 10 + Web 39） |
| launch 自动化 ACCEPT | 29 条（不计入真人） |
| 代理脚本/其他 | 14 条 |
| **真人 ACCEPT 职位对应的 impressions** | 538 条，其中 **457 条（85%）served_at 为 NULL** |

**结论**：served 率 5.4% 不是「顾问不看推荐」的单一原因——`markServed`（src/tier.js）只在 **Web 列表接口真实下发**时回填 served_at，**飞书卡片接单路径完全不经过它**。而 v3 归因已证明顾问主通道是飞书卡片（本人卡片接单存在）。两层证据合并：真人接单的职位大多来自 ttc-owner 同步/launch 建群等非推荐路径，且卡片路径的曝光永远不会被记为 served。specs/022 C 层的诊断动作应为：①给飞书卡片路径补 served 埋点；②推荐入口与实际接单路径的脱节本身是产品问题（顾问没有从推荐列表工作）。

### 5.2 补充验证（同日）：飞书每日小卡机制审核

**机制（代码 + push_log 实测证实）**：worker 每天 07:00 / 19:00 CST 两波调度（实测 403 条 SENT 全落在这两个钟点），按每顾问最新 COMPLETED 的 decision_runs 快照（2h 节流 + input_hash + complete=1 前置）取 Top3 建卡，`UNIQUE(consultant_id, kind, run_id)` 幂等（run_id 为波次键 `YYYY-MM-DD#HHMM`）。卡含总分/置信/档位/依据（关系事件计数 + 群活跃）/风险/下一步（工作台接单），接单走 `bot:accept:` 一键链路。另有无变化不推的 autopush（默认关，`BRAINX_PUSH_AUTO`）。近 7 天：DAILY_TOP3 126 条 SENT、STAGE_REMINDER 329 条 SENT——**推送机械面健康**。

**验证发现 4 个问题**：

1. **档位塌缩：89% 输出 OBSERVE**。全表 5,791 条推荐：OBSERVE 5,217 / RECOMMEND_WATCH 574 / RECOMMEND_ACCEPT **30**。卡片位（rank≤3）近 7 天 81 条中 OBSERVE 69（85%）——卡面写「今天建议优先处理」，实际几乎全是观察档，可执行的 ACCEPT 档基本到不了卡片。
2. **ACCEPT 档只有 linda 能产出**：30 条全部来自 linda 今天 3 轮（脑利 Agent 工程师 / 恒星力量 PR 主管 / 脑利 CEO 助理——direction 100 + 高活跃）。其余 8 位顾问历史为 0。原因：linda 盘子小（3 个有效 membership）但个个高匹配；大盘顾问（york 189 个）职位证据覆盖率不足 → coverage 拉低 → 强制 OBSERVE。**这是「顾问不从推荐列表接单」的算法侧解释**：不是顾问不用，是卡片上没有可执行档位。
3. **审计链断点**：`push_log.run_id` 是波次合成键，与 `decision_runs` 的 UUID 无法 JOIN——卡片发出的职位无法回溯推荐依据，只能按时间+顾问模糊对齐。specs/019 账本设计应吸收（push_log 记 decision run 引用）。
4. **3 条 FAILED 为数据污染非机制故障**：wendy 两个 demo 假群（`oc_demo_src_TTC-*`）+ linda 一个 bot 已退出的群。demo 种子数据的 project chat 不应进推送目标，需过滤或清理。

**与 §5.1 合并的结论**：served 5.4% = 埋点缺失（卡片路径无 markServed）+ 档位塌缩（卡片无 ACCEPT 可点）+ 入口脱节（接单来自推荐外路径）三因叠加。specs/022 C 层优先级排序：卡片 served 埋点 → 档位阈值放宽（coverage 惩罚在冷启动期过重）→ push_log 审计链 → demo 目标清理。

## 6. 节点清单（验收节点：✅ 已过 / ⬜ 未过）

**数据地基（本评判范围）**
1. ✅ 第一批全量 GLM 清洗：8,131 条判定 0 失败（4.5h，`gpt-5.6-luna`）
2. ✅ 平反回写：1,340 条 llm-recovery UPDATE 进 pending（partial-unique-index 约束下 UPDATE-原行设计）
3. ✅ 规则 v2 清单 + 词表 top40（docs/2026-09-24-cold-start-rules-v2-profiles.md）
4. ✅ 客户画像 141 家写库（migration 0054，幂等）
5. ✅ 顾问画像 v3 渠道归因（经用户本人复核，v1 三处证伪已记录）
6. ✅ 第二批回填：922 条入 `lark_messages`（origin='backfill'），13 家新公司进入数据面

**决策闭环（未启动/进行中）**
7. ⬜ 顾问人工确认 1,737 条 pending —— **阻塞节点**：确认量直接决定 LTR 重导样本量
8. ⬜ LTR 重导（等节点 7 产生确认数据；当前 203 行 NDCG@10 0.61 不可作为投产依据）
9. ⬜ 规则 v2 词表并入规则引擎（specs/003）＋ FAMILY_RULES 正则表落仓库（现只在已删的临时脚本里，文档引用悬空——本次评判发现，需修复）
10. ⬜ specs/022 施工（US1 信号层 → US2-4 特征层 → US5-7 时序层；kill-switch `BRAINX_LLM_CLEANUP=1`）
11. ⬜ `launch_auto_accepted` 事件拆分（specs/019 吸收，防画像再污染）
12. ⬜ routed push 灰测（等节点 8+10）
13. ⬜ 服务率 5.4% 诊断（specs/022 C 层：served 4.3-5.4% 意味着 95% 的推荐从未送达）
14. ⬜ OSS 异地备份启用（脚本/单元已部署，等控制台 RAM 角色绑定 + 建桶）

## 7. 数据可信度声明

| 数据 | 可信度 | 依据 |
|---|---|---|
| GLM 清洗判定 | 高 | 全量、0 失败、规则对账 4,842 条一致 |
| 顾问画像 v3 | 中高 | idempotency_key 前缀归因 + 用户本人复核；代点权重 ×0.5 是估计 |
| 客户画像 | 中 | 词表规则聚合，未分类 29 家影响族归属但不影响覆盖判定 |
| 第二批族分布 | 中低 | 规则直抽、无 GLM 层，群名偏置已知；只作验证不作生产口径 |
| 服务率 5.4% | 高 | 表内直接计数 |
