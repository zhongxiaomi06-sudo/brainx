# 2026-09-12 冲刺改动：职位详情页一键接单与字段精简（T1/T2）

> 上级：[前端审核台账](README.md) · 来源：[0.9 冲刺交付清单](../2026-09-12-sprint-delivery-checklist.md) T1/T2

## 改动范围

- **T1 回放 tab 下线**：`job-detail-card-review.tsx` 删除「回放」tab 与恒为空的决策回放占位区；历史入口（通知、深链）统一落到判断页/决策轨迹，不再出现空 tab。详情页主按钮改为「一键接单」：一次点击 = 职位入项目 + `launchProjectWorkflow` 拉起飞书群 + 机器人推职位卡，全程无表单、无二次确认。
- **T2 字段精简**：详情页删除「核心职位事实」区 6 个字段（职位编号/主做顾问/与我的关系/当前阶段/客户类型/优先级）；判断页评分摘要不再显示证据覆盖与策略版本，六维评分行不再显示权重。首屏只剩公司名、岗位名、base 地、招聘状态、HC。
- 同步改动：`workbench.tsx` 一键接单接线与回放重映射、`engagement-loop.tsx` 开始跟进免表单（T5，单独 commit）、OpenMai 文案统一（T8，单独 commit）。

## 五维状态

| 维度 | 状态 | 证据 |
|---|---|---|
| Storybook | 已完成 | `job-detail-card-review.stories.tsx` 场景同步更新（按钮改名、tab 列表去回放），前端 44/44 测试通过 |
| 用户审核 | 未审核 | 演示前排期改动，待用户复看 |
| 正式接入 | 已接入 | 改动即正式入口（`workbench.tsx:384` 一键接单接线） |
| 目标环境发布 | 未发布 | 待生产 build 后回填 commit 与时间 |
| 真实数据验证 | 未验证 | 待灰测：真实职位一键接单 → 飞书群拉起 → 职位卡到群 |

## 未确认项

- 「一键接单」拉群失败时的前端兜底文案未单独验收（当前沿用 `launchProjectInFeishu` 既有错误通知）。
- 判断页是否按 T3 进一步改为「静态匹配要点 + 跳转链接」属后续任务，本记录不含。
