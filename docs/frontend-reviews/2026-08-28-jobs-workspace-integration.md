# 现代职位工作区正式接入审核记录

> 上级入口：[前端审核台账](README.md)

## 审核身份

- 审核日期：2026-08-28
- Storybook 场景：`组合场景/现代职位工作区审核稿/桌面端主审核稿`、`正式页面嵌入模式`
- 对应 commit：本次 `feat(frontend): 正式接入现代职位工作区`
- 审核范围：现代职位工作区的绿色视觉、列表结构、筛选、整行详情和正式“全部职位”入口

## 用户结论

- 已确认：保留绿色视觉；现代职位工作区审核效果可以进入正式版本；正式页面应复用已经统一的居中职位详情卡。
- 未确认：目标环境发布效果、真实账号下的所有空值和异常情况、3000 条以上职位的连续加载体验。
- 正式接入授权：允许接入正式“全部职位”入口；不授权在本次修改推荐策略、方向画像或后端业务规则。

## 数据与动作边界

- 真实字段：职位编号、职位、公司、城市、TTC 状态、HC、Pipeline、主做顾问、最近更新、项目关系、客户类型、优先级、当前阶段、下一步、备注、来源链接和承接状态。
- 缺失字段：继续显示“待确认”或空状态，不补造职位类型、综合分、招聘意愿、行业或转化率。
- 后端依赖：列表与字段能力来自 `GET /api/v1/radar`；完整详情来自 `GET /api/v1/opportunities/:id`；加入项目复用现有项目归属接口。
- 允许动作：搜索、后端能力允许的城市/状态筛选、最近更新排序、分批加载、整行或 Enter 打开详情、加入我的项目；没有真实回调或来源 URL 时不显示同步、暂不考虑或查看来源动作。

## 状态证据

- 正式入口：`workbench.tsx` 的“全部职位”进入 `WorkbenchJobsPage`，该页面直接复用 `JobsWorkspaceReview` 的嵌入模式，不再渲染旧 `TtcJobsTable`。
- 发布环境与版本：未发布；本记录只证明本地正式代码接入。
- 自动验证：`npm run verify:quick` 16/16，前端静态与适配测试 29/29；完整门禁结果在本次提交后补充。
- 真实数据验证：适配层与接口契约已验证；目标环境真实职位数量、空值、详情失败和加入项目结果仍待复核，因此记为“部分验证”。

## 未完成项

- [ ] 在目标环境确认本次 commit 已发布。
- [ ] 使用真实账号核对列表总数、字段空值、禁用筛选和连续加载。
- [ ] 从列表打开同一职位，核对详情接口成功与失败时的展示。
- [ ] 核对“加入我的项目”成功、重复操作和失败反馈。

## 相关文档

- [内部 Storybook 组件库](../storybook-component-library.md)
- [前端真实数据重构施工清单](../frontend-refactor-construction-checklist.md)
- [统一职位详情正式接入审核记录](2026-08-28-job-detail-unification.md)
- [上传前完整验证](../standards/PRE_PUSH_VERIFICATION.md)
