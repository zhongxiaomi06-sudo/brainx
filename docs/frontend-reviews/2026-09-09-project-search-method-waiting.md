# 项目群等待选择找人方式复核

> 上级入口：[前端审核台账](README.md)

## 审核身份

- 审核日期：2026-09-09
- Storybook 场景：本轮只调整正式项目启动状态与工作台文案，未新增独立视觉组件
- 对应 commit：本记录所在提交
- 审核范围：创建飞书项目群后的找人触发时机

## 用户结论

- 已确认：建群之后不能直接启动 OpenMai，应先让顾问选择 OpenMai 或 SuperMai。
- 已确认：后续应提供可选的找人条件输入；未填写时由机器人根据职位信息自动找。
- 未确认：双按钮、输入框以及飞书卡片的最终视觉和交互细节。
- 正式接入授权：本轮允许先取消自动 OpenMai 并进入等待选择状态；双入口交互另行实现和审核。

## 数据与动作边界

- 真实字段：项目、职位、项目群、协作者、项目启动状态。
- 缺失字段：本轮找人方式和本轮附加条件尚未形成正式交互输入。
- 后端依赖：后续需要把明确选择映射到 OpenMai 或 SuperMai 的既有找人工具。
- 允许动作：建群、投放职位卡、接单并返回 `awaiting_method`；不得在该步骤检查 TTC 凭证、启动 OpenMai 或产生找人费用。

## 状态证据

- 正式入口：`ProjectsView` 的飞书项目启动动作，以及项目启动服务。
- 发布环境与版本：仅本地代码，未发布目标环境。
- 自动验证：项目启动回归覆盖首次建群、复用已建群、缺确认和历史失败状态，确认均不会自动调用 OpenMai。
- 真实数据验证：未执行真实建群或真实找人写操作。

## 未完成项

- [ ] 在项目群内提供 OpenMai 与 SuperMai 两个按钮。
- [ ] 增加可选找人条件输入，留空时根据职位信息自动生成。
- [ ] 在真实飞书桌面端和手机端完成视觉、按钮回调与权限验证。
