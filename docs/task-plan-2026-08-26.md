# 明日改进任务排期（2026-08-26）

来源：08-24/08-25 两轮审计 + 生产验证遗留。按优先级排序，标注负责人候选与验收标准。

## P0 — 上午必做（数据闭环）

### 1. TTC 限流恢复确认（30min，自动检查已挂 20:48，明早复核）
- 验收：`sync_runs` 出现晚于 08-25 11:40 的 `source='ttc' complete=1` 行；`ttc_rr` 游标在 0-5 间轮转；推荐快照时间从 08-21 前进
- 若仍 -90429：确认 fail-fast 每轮仅 1 请求；联系 TTC 侧确认租户配额窗口/是否需人工解限
- 相关：docs/cloud-recovery-checklist.md

### 2. york-ai 加 Bitable 协作权限（10min，人工/飞书后台）
- 把 york-ai 应用（cli_aaf72a911bb9dd21）加为「职位盘点」Base 协作者 → bot 兜底通道恢复（实测 91402）
- 验收：`lark-cli --profile york-ai base +record-list --base-token bItkMlfbQWHXnfsT6LBcWez3nZg --table-id tblGScnNnl1Z7tDQ --page-size 1` 返回 200

### 3. 打标下发（核心，决定算法迭代节奏）
- `npm run data:pull` 拉最新库 → `npm run data:audit` 出缺口矩阵（基线：123/140 缺口）
- `npm run label:export -- --db data/brainx-cloud.db` 生成 7 人标注表 → 发飞书群
- 话术模板与标准：docs/labeling-standard-v1.md（10 分钟/人，felix/shanon/mia 已标过的可抵扣）
- 验收：回收 ≥5 人 → `npm run label:import -- --db <回灌库>` → audit 缺口 <40

## P1 — 下午（工程健康）

### 4. retention 周度例行化
- 云端 cron：`0 4 * * 1 cd /opt/brainx && node bin/brainx-retention.mjs --apply >> logs/retention.log`
- 先行 dry-run 核对计数；bridge-error 观测行纳入保留规则（当前规则不删它们，会累积）

### 5. 敏感数据与仓库卫生（审计 P0/P1）
- git history 中的客户 CSV×2 + PRD PDF + prd_extracted.txt：确认可公开性，不可则 BFG/filter-repo 清洗 + 强推（需全员同步）
- `_archive/`（203 文件）与 `.prd_pages/`：移到独立 archive 仓库或删除
- 删除冗余 CI：`pr-test.yml`（ci.yml 已覆盖，每次跑两遍测试）

### 6. CODEOWNERS + 评审路由
- 前端 → jiands233/Mia；src/scorer+recommend+sync → hanyu；migrations+scripts+bin → 曾老师
- 配合已开启的分支保护（require_code_owner_reviews 可随后打开）

## P2 — 本周内（算法与性能）

### 7. 第一次权重网格搜索（依赖 #3 打标回收）
- 用冻结推荐 + 打标/行为标签跑粗网格（docs/training-requirements.md §第四步）
- 产出：baseline-1.2 权重候选 + 影子对比报告

### 8. sync.js feishu 路径异步化（B11 遗留）
- runSync 同步签名波及面大，先只把 fetchFeishuJobs 的 execFileSync 换成 worker_threads 隔离

### 9. systemd 小修
- unit 加 `KillMode=mixed`（restart 不再 SIGKILL 超时）
- brainx-guard 加磁盘水位检查（df <15% 告警到飞书）

## 观察项（无需动作，看数据说话）
- 19:00/07:00 推送窗口 push_log 全 SENT（B1/B4/B11 已验证一轮）
- recommendations 日增行数（节流后应 <3 万/天，超限回头查 throttle）
- 打标回收率与「不感兴趣」原因分布（反哺方向维同义词表）
