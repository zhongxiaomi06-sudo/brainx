# 业务工作全景（全链路 × 工具 × 权限）

> 上级入口：[BrainX 文档书](README.md) · [OpenClaw 壳子架构](2026-09-02-openclaw-shell-architecture.md)
>
> 依据：2026-09-02 16:27「研发对一下」（York 姚堃 × Mia 钟笑咪，妙记 `obcnsf91z37eqqv8d87f591q`）+ 现有 PRD。
>
> 定位：**回答"这个架构到底承担哪些业务工作"**。每一件业务工作明确：谁做、走哪个工具、需要什么飞书权限、当前能不能跑。
>
> ⚠️ **9/2 晚产品定位更新**：**一面是这个产品的分界线**——一面之前是 **AI leader 工作流**（主动串联推进），一面之后是**日历助手**（只做 "你什么活没干" 的提醒）。两种形态的性格、自动化程度、现有家底完全不同，详见 **[AI leader 工作流 + 日历助手](2026-09-02-ai-leader-workflow.md)**。本文 §1-§2 侧重前半段。

## 1. 主链路：猎头全链路六段

会上 York 明确的全流程：**职位信息录入 → 接单判断 → 候选人推荐 → 面试跟进 → offer 谈判 → 入职**。

> **分界线（9/2 晚明确）**：**⑥ 一面之前是 AI leader 工作流，一面之后是日历助手。** 见下表，两种形态性格不同。

| | ①~⑥ 一面之前 | ⑥ 一面之后 |
|---|---|---|
| 性格 | **AI leader**：主动串联推进 | **日历助手**：只提醒不代做 |
| 自动化 | 高 | 低 |
| 现有家底 | 断的（**约面/一面零代码**） | **基本齐全** |
| 详情 | [AI leader 工作流 + 日历助手](2026-09-02-ai-leader-workflow.md) | 同左 |

| # | 业务工作 | 现状 | 承担者 | 走哪个工具 | 需要什么权限 |
|---|---|---|---|---|---|
| 1 | **职位信息录入** | 合伙人在职位群发职位 | 人 → AI 提炼 | 待建（群消息提炼） | **读群全量消息**（高敏感） |
| 2 | **接单判断 / 承接** | 已建成 | 人（顾问确认） | `brainx_engage` (ACCEPT) | 发消息（低） |
| 3 | **候选人推荐** | 已建成 | AI 跑，人确认 | `brainx_recommendations` / `brainx_recommend_run` | 发消息（低） |
| 4 | **面试跟进** | 部分 | 人 + AI 提醒 | `brainx_record_progress` | 发消息（低） |
| 5 | **offer 谈判** | **数据盲区** | 人（私聊/小群） | 无 | 读私聊/小群（高敏感，未批） |
| 6 | **入职 / 关闭** | 已建成 | 人 | `brainx_terminal_result` | 发消息（低） |

> **关键缺口**：公司现有驾驶舱**只能监控到一面、二面**，**三面及 offer 谈判阶段全在内部小群和私聊里，没有统一线上数据源**。这是会上点名的数据盲区，也是这套系统相对 DataClaw 的差异化机会——但要吃到它必须拿到私聊/小群的读取权限，敏感度比群消息更高。

## 2. MVP 每日循环（会上定的核心节奏）

这是第一版要跑通的东西，按时间顺序：

```text
每天
 ├─ ① AI 推 10 个岗位 → 顾问对话窗口
 ├─ ② 顾问点确认
 ├─ ③ 自动起后台找人任务（OpenMai / reloop）
 ├─ ④ 候选人结果推回对话窗口
 ├─ ⑤ 顾问选择「接受」或「回流重筛」
 ├─ ⑥ 不成功自动起下一轮
 └─ ⑦ 顾问当日需在指定群内报备推送人数（人工，因接口读不到外部群）
```

| 步 | 业务工作 | 承担者 | 工具 | 阻塞项 |
|---|---|---|---|---|
| ① | 每日推 10 个岗位 | AI | `brainx_recommendations` + 推送通道 | 定时任务机制未定（OpenClaw cron? 自研?） |
| ② | 顾问确认 | 人 | 对话（@机器人 即可） | — |
| ③ | 后台找人 | AI | `startOpenmaiTask`（**未暴露到 MCP**）+ reloop | **P0-1：OpenMai 工具未注册** |
| ④ | 结果推送 | AI | `brainx_openmai_result`（**未暴露，且需脱敏**） | P0-1 + 脱敏脚本 |
| ⑤ | 接受 / 回流重筛 | 人 | `brainx_feedback` | 待核职位归属校验 |
| ⑥ | 自动起下一轮 | AI | `brainx_recommend_run` | 需加频率限制 |
| ⑦ | **群内报备推送人数** | **人（每日）** | 纯人工发消息 | **接口读不到外部群消息** —— 这是⑦存在的原因 |

> **⑦ 是妥协，不是设计。** 因为"系统暂无法自动读取所有外部群消息"，才让顾问每天手动报备。若能拿到读群全量消息权限，⑦ 可自动化 —— 这也是那条高敏感权限的价值所在。

## 3. 支撑类业务工作（会上提到但不在主链路）

| 业务工作 | 会上怎么说的 | 现状 | 需要什么 |
|---|---|---|---|
| **群信息标准化字段提炼** | "可通过开源算法 + 自研 agent 实现，MVP 可跑通" | 待建 | 读群全量消息 + 字段抽取规则 |
| **目标检查** | "综合读取群聊信息、CRM 数据库数据核验工作" | 待建 | 读群消息 + CRM 接口 |
| **接单自动生成目标** | "关联接单自动生成目标，无需单独填报" | 待建 | 挂在 `brainx_engage` (ACCEPT) 后 |
| **数据验算 / 对账** | "己方推人、面试人数与对方做校验；后续与 DataClaw 对账" | 待建 | 索要对方接口 → agent 跑数 → 交叉校验 |
| **绩效周报** | DataClaw 现有能力，每日定时汇总群信息、提醒目标填报 | 已有（对方） | — |
| **拉群承载独立事项** | York 想要，第二版 | 未做 | `im:chat:create`（**待审批**） |
| **项目 Sheet 管理列表** | 解决多项目对话信息串扰 | 未做 | sheets 权限（第二版） |

> **数据验算的思路要记住**：会上明确是**"不用接口监控各群目标"**，而是拿己方数据与对方做**交叉验算**，不一致时人工纠正。这样既保证准确，又不用申请大量敏感接口。

## 4. 谁做什么：人 / AI / 外部系统分工

| 角色 | 承担的业务工作 |
|---|---|
| **合伙人** | 在职位群发职位（第 1 段输入源） |
| **顾问（用户）** | 确认岗位、接受/回流候选人、记录进展、offer 谈判、入职确认、**每日群内报备** |
| **AI（OpenClaw + Skill）** | 推岗位、起后台找人、结果推送提醒、进展提醒、群信息提炼、目标检查草案 |
| **BrainX MCP server** | 提供业务真值、裁决状态、守住隐私与归属（唯一写库通道） |
| **reloop** | 候选人域权威：身份、简历、匹配、触达、submission |
| **OpenMai** | 后台自动找人 |
| **DataClaw（外部）** | 绩效周报、现有定时任务；对账参照 |

**不变的原则**：AI 只提议，人做决策。所有写动作（接单、进展、终局）必须顾问显式触发，AI 不得代签。

## 5. 权限 × 业务工作 对照

哪些业务工作卡在哪条权限上（精确 scope 见 [飞书权限清单](2026-09-02-feishu-permission-scopes.md)）：

| 权限档位 | scope | 解锁的业务工作 |
|---|---|---|
| **低（建议立即申请）** | `im:message` `im:message.send_as_bot` `im:message.p2p_msg:readonly` `im:message.group_at_msg:readonly` `im:chat` `contact:user.base:readonly` `im:resource` | ①推岗位 ②确认 ⑤接受/回流 ⑦报备 —— **MVP 主循环可以完整跑通** |
| **高敏感（待审批）** | `im:message.group_msg` / `im:message.group_msg:get_as_user` | 职位录入自动提炼、群信息标准化、**目标检查**、⑦自动化 |
| **高敏感（第二版）** | `im:chat:create` | 拉群承载独立事项 |
| **中（第二版）** | sheets / drive | 项目 Sheet 管理列表 |
| **未讨论（盲区）** | 私聊 / 小群读取 | **offer 谈判段**（第 5 段数据盲区） |

> **好消息**：**MVP 主循环（①-⑦）只依赖低敏感权限**，不需要等审批就能跑通。高敏感权限解锁的是"锦上添花"的自动化（群信息提炼、目标检查、⑦ 自动化），可以并行申请、批下来再接。

## 6. 当前阻塞项汇总

按"阻塞哪件业务工作"排：

| 阻塞项 | 卡住的业务工作 | 工期 | 参考 |
|---|---|---|---|
| **OpenMai 工具未暴露到 MCP** | ③ 后台找人、④ 结果推送 | 0.5 天 | 下游文档 P0-1 |
| **`brainx_talent` 无 cid 隔离** | 候选人查询（禁止外露中） | 1.5 天 | 白名单 §4 |
| **`brainx_sync_now` 默认参数危险** | 挂 MCP 前的硬前置 | 0.2 天 | 白名单 §4 |
| **`brainx_record_outcome` 漏守门** | 结果录入 | 0.3 天 | 白名单 §4 |
| **人才库整库权限未批** | 候选人数据供给（现走临时共享方案） | 外部依赖 | 权限清单 §6 |
| **读群全量消息权限未批** | 职位提炼、目标检查、⑦自动化 | 外部依赖 | 权限清单 §6 |
| **reloop 桥 1 未开工** | 推人循环第一公里 | 2 天 | 架构文档 §8.2 |
| **定时任务机制未定** | ① 每日推 10 个岗位 | 0.5 天 | 待定（OpenClaw cron vs 自研） |

## 7. 待你确认的三个决策

| 决策 | 选项 | 影响 |
|---|---|---|
| **每日推 10 个岗位用什么定时** | OpenClaw 内置定时 / 自研 cron / 复用 DataClaw 定时任务 | 决定 ① 的实现方式与可用性 |
| **第一版是否等读群消息权限** | 不等（用⑦人工报备）/ 等 | 不等能立刻跑通，等则更完整但阻塞 |
| **offer 谈判段要不要纳入** | 纳入（需私聊权限，极敏感）/ 暂不纳入（承认是盲区） | 影响演示故事完整性 |

## 相关文档

- [OpenClaw 壳子架构](2026-09-02-openclaw-shell-architecture.md) — 技术分层与责任边界
- [飞书权限清单](2026-09-02-feishu-permission-scopes.md) — 精确 scope 与事件
- [工具外露白名单](2026-09-02-tool-exposure-whitelist.md) — 每个工具能不能外露
- [下游交付文档](2026-09-02-brainx-mcp-deliverable.md) — MCP server 契约与 P0 排期
- [Workflow Hub 与猎头全链路架构](workflow-hub-architecture.md) — 三方 ID 映射与状态机
