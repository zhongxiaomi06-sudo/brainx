# 客户洞察未知字段噪音清理复核

> 上级入口：[前端审核台账](README.md)

## 审核身份

- 审核日期：2026-09-15
- Storybook 场景：`ClientInsightsReview / VerifiedFacts`
- 对应 commit：本记录随 `fix(frontend): 清理客户洞察未知字段噪音` 一并提交
- 审核范围：正式“客户洞察”列表的客户类型占位与职位状态标签

## 用户结论

- 已确认：用户要求持续逐项改善，并在真实页面测试后截图；本轮结果已发当前对话。
- 未确认：用户尚未逐项确认客户表的最终密度与标签样式。
- 正式接入授权：允许；仅收敛无信息量的展示，不修改真实数据和下钻动作。

## 数据与动作边界

- 真实字段：`companyType`、`states`。
- 缺失字段：客户类型为空时列表不再重复“类型未标注”；详情仍显示“客户类型未标注”。状态只有 `UNKNOWN` 时显示“待确认”，与已知状态共存时只展示可解释的已知状态。
- 后端依赖：无新增依赖，源枚举与接口契约保持不变。
- 允许动作：搜索、筛选、排序、打开客户事实和下钻客户职位保持不变。

## 状态证据

- 正式入口：主导航“客户洞察”的 `ClientInsightsReview`。
- 发布环境与版本：本地开发环境；目标环境未发布。
- 自动验证：Storybook 86/86；生产构建成功。
- 真实数据验证：本地真实接口返回 4020 个客户、23245 个职位，首屏 100 行；可见正文 `UNKNOWN` 0 个、“类型未标注”0 个，截图已发当前对话。

## 未完成项

- [ ] 用户复看清理后的客户列表信息密度。
- [ ] 目标环境发布后复核纯未知状态和存在客户类型的记录。
