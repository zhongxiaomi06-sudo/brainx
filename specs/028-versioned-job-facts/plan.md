# 028 Plan — 来源适配与版本化职位事实

1. 冻结 `runSync`、TTC 分页、CSV adapter、群/JD 草稿和确认链的现有行为。
2. 先写统一信封、事实版本、证据、缓存、冲突和对账的失败回归。
3. 以追加迁移建立来源记录、不可变事实版本、字段证据、冲突与提炼缓存；不执行生产迁移。
4. 新增零依赖来源契约和职位事实写模块，由 `runSync` 与 `confirmDraft` 共同复用。
5. 让 TTC、本地 CSV/fixture 显式携带来源身份；保留旧 payload 兼容规范化。
6. 为群消息/JD 提炼增加按租户与授权范围隔离的内容哈希缓存，继续由草稿确认门控制权威写入。
7. 提供投影对账与默认 dry-run 补偿函数，验证不会改写关联对象或历史版本。
8. 运行专项、快速和完整门禁，更新施工手册与验证记录。

不新增运行时依赖，不改变推荐算法、正式读路径、外部接口或生产数据。
