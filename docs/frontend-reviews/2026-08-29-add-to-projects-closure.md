# 2026-08-29 加入我的项目闭环正式接入记录

> 上级入口：[前端审核台账](README.md)

## 审核身份

- 审核日期：2026-08-29
- Storybook 场景：`业务组件/今日决策队列/正式入口复用 V2 卡片`、`组合场景/应用外壳/FullWorkbench`
- 对应 commit：本记录所在原子提交
- 审核范围：推荐卡片与全部职位加入项目、成功反馈、去我的项目、刷新后可见、我的项目真实摘要

## 用户结论

- 已确认：用户要求按最终施工手册直接开始本地施工，并优先完成“加入我的项目”闭环。
- 未确认：新版“我的项目”卡片的最终视觉密度、五种状态分组和目标环境效果。
- 正式接入授权：允许；范围限定为本地正式入口的加入项目闭环，不包含远端同步、推荐分页和推荐算法调整。

## 数据与动作边界

- 真实字段：项目归属、加入时间、公司、职位、城市、职位状态、HC、Pipeline、当前阶段、当前行动、截止时间和 engagement 状态。
- 缺失字段：完整分页、分组数量、阻塞解除摘要和终局回放摘要仍待后续契约完善。
- 后端依赖：`PATCH /api/v1/opportunities/:id/membership` 与新增 `GET /api/v1/projects`。
- 允许动作：加入我的项目、去我的项目、开始跟进或打开项目；观察和开始跟进仍保持独立语义。

## 状态证据

- 正式入口：今日决策 `ADD`、职位详情和全部职位均调用同一个 `addToMyProjects`；我的项目直接消费 membership 摘要。
- 发布环境与版本：仅当前本地功能分支，未发布目标环境。
- 自动验证：项目归属 HTTP 测试 2/2、前端静态与适配 30/30、Storybook 74/74、快速门禁 16/16 通过。
- 真实数据验证：本轮用隔离数据库验证加入、重复幂等、重新读取和待开始状态；尚未使用目标环境真实账号验证。

## 已完成

- [x] membership 响应返回最新项目摘要。
- [x] 加入后 `GET /api/v1/projects` 立即可见，重复请求不产生第二条活跃归属。
- [x] 正式推荐卡 `ADD` 写入 membership，`GO_PROJECT` 导航并定位项目。
- [x] 全部职位和详情入口复用同一加入动作。
- [x] 我的项目不再使用 `ACCEPTED` 列表或前端伪条目。

## 未完成项

- [ ] 在目标环境用真实顾问和真实职位复核完整路径。
- [ ] 补齐网络失败分类、显式重试和冲突恢复交互。
- [ ] 按待开始、跟进中、需处理、已完成和已释放完成分组与 Storybook 全状态审核。
- [ ] 接通开始跟进后的项目摘要即时刷新和完整行动闭环。

## 2026-08-29 后续施工进展

- 已在当前本地分支确认：开始跟进表单强制填写目标、第一行动和截止时间，后端在同一事务内写入 `ACCEPTED` 与唯一当前行动。
- 已接通即时刷新：写入成功后重新读取正式工作台和项目摘要，项目状态由待开始更新为跟进中；职位详情继续读取后端 OpenMai 状态，不在前端伪造结果。
- 已统一正式界面、统一职位详情和 Storybook 的用户语言为“加入我的项目 / 关注 / 开始跟进 / 跟进中 / 结束跟进”；后端机器状态与已有数据结构保持不变。
- 已有隔离数据库测试覆盖必填校验、原子写入、幂等、进展换行动、终局限制、释放取消行动和跨顾问读取；仍需目标环境真实账号复核、阻塞态完整视觉审核和冲突恢复交互。
- 已将正式“我的项目”抽成独立生产组件 `ProjectsView`：五种状态具有真实数量和筛选入口，需处理与较早截止的项目优先，行动行显示目标、当前行动、截止/阻塞提示和明确下一步。
- 新增 Storybook 五状态、空状态、刚加入定位、筛选动作和窄屏场景；自动交互已通过，但用户尚未审核本轮视觉，目标环境也未发布。
- 本轮没有伪造“最近进展”：当前项目摘要尚未返回最近 outcome，卡片只展示已有行动和状态；最近进展字段仍列为后端待办。

## 相关文档

- [BrainX 最终交付蓝图与施工总清单](../brainx-final-delivery-blueprint.md)
- [推荐队列与职位决策产品架构](../recommendation-queue-product-architecture.md)
- [前端真实数据重构施工清单](../frontend-refactor-construction-checklist.md)
- [内部 Storybook 组件库](../storybook-component-library.md)
