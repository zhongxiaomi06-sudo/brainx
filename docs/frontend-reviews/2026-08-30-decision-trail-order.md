# 决策轨迹时间顺序复核

> 上级入口：[前端审核台账](README.md)

## 审核身份

- 审核日期：2026-08-30
- Storybook 场景：`业务组件/居中职位事实卡审核稿/决策轨迹最新在前`
- 对应 commit：本次“修复决策轨迹时间倒序”原子提交
- 审核范围：今日决策与全部职位共用详情卡中的决策轨迹展示顺序

## 用户结论

- 已确认：用户明确要求决策轨迹从最新时间向下排列。
- 未确认：高频自动“已推荐”历史噪声的最终清理方案另行审计，不包含在本次排序提交中。
- 正式接入授权：允许；共用生产组件可直接修复。

## 数据与动作边界

- 真实字段：事件类型、发生时间、原因。
- 缺失字段：事件没有时间时放在有时间记录之后，不补造时间。
- 后端依赖：`GET /api/v1/opportunities/:id` 返回真实 `events`。
- 允许动作：只改变展示顺序，不改写事件账本。

## 状态证据

- 正式入口：两个职位入口都复用 `JobDetailCard`，由组件统一按事件时间倒序后取最近三条。
- 正式今日决策的自定义详情内容也复用同一 `newestEvents` 规则，避免包装层覆盖组件页签后重新显示完整升序事件。
- 发布环境与版本：当前本地分支，未发布目标环境。
- 自动验证：Storybook 使用故意乱序的四条记录，断言只显示最新三条且严格为新到旧。
- 真实数据验证：待本地正式页面复核。

## 未完成项

- [x] 完成高频自动推荐事件审计；本地 339,662 条机器轨迹已清理，生成链路已切断。
- [ ] 在本地真实职位中复核跨日期事件的倒序结果。
- [ ] 目标环境发布后用真实 OAuth 账号复核。

## 同日补充：机器推荐轨迹清理

- 推荐快照仍保存在 `recommendations`，不再复制为“已推荐”决策事件。
- 决策轨迹只保留真实人工操作与事实更新；前端无需隐藏或伪造数据。
- 本地真实库清理后剩余 64 条人工事件，机器 `RECOMMENDED` 为 0；目标环境尚未验证。
- 根因、计数和发布边界见[高频自动推荐审计](../audits/2026-08-30-recommendation-event-growth.md)。

## 相关文档

- [统一职位详情正式接入审核记录](2026-08-28-job-detail-unification.md)
- [跟进操作区与暂不考虑确认层复核](2026-08-30-engagement-action-layout.md)
- [内部 Storybook 组件库](../storybook-component-library.md)
