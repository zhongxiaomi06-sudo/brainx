# BrainTex 富卡片与业务闭环审核记录

> 上级入口：[前端审核台账](README.md)

## 审核身份

- 审核日期：2026-09-03
- Storybook 场景：未开始；回复卡由锁定版 OpenClaw 飞书渠道渲染，职位操作继续复用正式工作台
- 对应 commit：本轮待创建的“OpenClaw 招聘闭环”提交
- 审核范围：普通回复卡片化、推荐卡信息密度、推荐设置入口、职位工作台深链与候选推进底层状态

## 用户结论

- 已确认：卡片必须精炼但有完整依据、风险和下一步；推荐数量及时间可自然语言设置；点击按钮进入既有工作台；接单、找人、联系人和候选推进尽量自动化；自动建群后置。
- 已验证：飞书桌面端真实 3 职位卡能显示标题、依据、风险、下一步、“联系人和推进”、回放及工作台按钮。
- 未确认：手机端、长内容折叠、最多 10 个职位时的体验，以及候选流程专页视觉。
- 正式接入授权：允许接入正式 OpenClaw 插件与现有工作台；外部联系发送和建群不在本轮自动执行授权内。

## 数据与动作边界

- 真实字段：冻结推荐、职位事实、负责人显示名、承接状态、行动、OpenMai 状态、候选 Case 状态和独立授权联系方式。
- 缺失字段：没有外部联系渠道发送回执；没有建群连接器；候选 Case 暂无专门前端页面。
- 后端依赖：Agent Gateway、正式工作台 HTTPS、OpenMai、人才 `contact` 授权。
- 允许动作：确认后保存设置、接单并找人、记录进展、推进候选状态；卡片 URL 本身不执行写入。

## 状态证据

- 正式入口：`plugins/brainx-openclaw/response-card.js`、`src/push.js`、`src/agent-gateway/` 与现有工作台对象深链。
- 发布环境与版本：本机 OpenClaw 目标环境已安装 `brainx-openclaw` 1.1.5；网关启动时加载 BrainX、Feishu 和 memory-core。
- 自动验证：原生推卡、动态数量、分钟级时间、工具白名单、联系人边界、接单找人、进展和候选状态专项测试通过。
- 真实数据验证：2026-09-03 17:31 向本人飞书发送真实 3 职位互动卡成功，message id 为 `om_x100b66a7a9b0ecb8b3923626033d515`；未执行任何业务写动作。
- 已知差异：当前 OpenClaw 版本的 Feishu 普通自动回复没有把 `replyPayloadSendingHook` 传入最终发送器；插件已按 typed hook 注册并在启动加载，但普通自然语言回复仍降级为文本。每日自动推荐改走已验证的 BrainX 原生互动卡通道，不把普通回复误报为通过。

## 未完成项

- [x] 安装新版插件并重启目标 OpenClaw Gateway。
- [x] 用真实飞书私聊检查原生推荐卡视觉和按钮。
- [ ] 修复或升级 OpenClaw Feishu 普通回复投递路径，再验证所有自然语言回答统一卡片化。
- [ ] 验证自然语言设置后下一次调度按新时间和数量推送。
- [ ] 为候选 Case 增加正式工作台操作区并单独审核。
- [ ] 接入外部联系发送回执；自动建群继续后置。

## 相关文档

- [OpenClaw 招聘闭环](../2026-09-03-openclaw-recruiting-loop.md)
- [飞书卡片 HTTPS 深链接入记录](2026-09-03-feishu-card-deep-links.md)
