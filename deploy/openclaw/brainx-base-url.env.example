# 安装为 /etc/brainx/base-url.env，权限 0640 root:brainx。
#
# 为什么要单独成文件：brainx-agent-gateway 以 User=brainx 运行，读不到
# /opt/brainx/.env（root:600，内含 BRAINX_DEV_AUTH 等敏感项，禁止整体挂载），
# 而工作台深链与项目群卡片生成都需要生产 HTTPS 基址。
#
# 缺失后果：brainx_bind_group_project / 发卡等工具会以 BRAINX_BASE_URL_REQUIRED
# 失败（2026-09-11 felix 在「linda -投放增长转项群」绑定失败事故）。
#
# 约束：必须是 https 且不含账号密码（见 src/brainx-deep-links.js#productionBaseUrl）。
# 本地联调若要放行 http://127.0.0.1，另设 BRAINX_ALLOW_HTTP_LOOPBACK=1。
BRAINX_BASE_URL=https://replace-with-production-https-origin
